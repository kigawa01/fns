ktor:
  development: true
  deployment:
    watch:
      - classes
      - resources
    port: 8080
  application:
    modules:
      - net.kigawa.fns.backend.service.ModulesKt.loadModule

jwt:
  secret: ""
  issuer: "ISSUER"
  audience: "audience"
db:
  host: localhost:3006
  port: 3006
  database: fns
  user: fns
  password: fns-pass
